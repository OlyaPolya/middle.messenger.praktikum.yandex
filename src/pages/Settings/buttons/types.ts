import Button from '../../../components/Button/Button';

type ButtonProp = {
  changeData?: Button;
  changePassword?: Button;
  signOut?: Button;
  save?: Button;
  attr: {
    class: string;
  };

};

export default ButtonProp;
